spring:
  data:
    mongodb:
      uri: mongodb://${MONGO_HOST:mongodb}:${MONGO_PORT:27017}/${MONGO_DB:chat_db}
    redis:
      host: ${REDIS_HOST:redis}
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:kafka:9092}
